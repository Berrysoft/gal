[package]
name = "random"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
gal-bindings = { path = "../../gal-bindings" }
num-bigint = { version = "0.4", features = ["rand"] }
rand = "0.8"
lazy_static = "1.4"
